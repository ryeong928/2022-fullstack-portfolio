import React from "react"
import { BaseScreen, PointFont } from "../../lib/styled"

export default () => {
  return(
    <BaseScreen>
      <PointFont>내가 쓴 글</PointFont>
    </BaseScreen>
  )
}