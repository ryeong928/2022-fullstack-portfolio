import React from "react"
import { BaseScreen, PointFont } from "../../lib/styled"

export default () => {
  return(
    <BaseScreen>
      <PointFont>구매기록</PointFont>
    </BaseScreen>
  )
}